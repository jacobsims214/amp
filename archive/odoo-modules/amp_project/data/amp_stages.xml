<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <data noupdate="1">
        <!-- Note: AMP Project uses state fields (Selection) rather than stages -->
        <!-- These are the state values for reference -->
        
        <!-- Project States: draft, active, archived -->
        <!-- Epic States: backlog, planning, in_progress, completed, cancelled -->
        <!-- Story States: backlog, ready, in_progress, review, completed, blocked -->
        <!-- Task States: backlog, ready, in_progress, review, completed, blocked -->
        
        <!-- Default project template -->
        <record id="project_template_amp_default" model="amp.project">
            <field name="name">AMP Default Template</field>
            <field name="description">Default project template for AMP with standard workflow</field>
            <field name="state">draft</field>
            <field name="active" eval="False"/>
        </record>
    </data>
</odoo>
